# Python packages bundled into the TripGo APK by Chaquopy.
#
# Only pure-Python packages that the backend actually imports at runtime. The
# desktop requirements.txt additionally carries psycopg2/Pillow/qrcode, which have
# no Android build and are unused by the API code.
Django>=5.0,<5.2
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
python-dotenv>=1.0
